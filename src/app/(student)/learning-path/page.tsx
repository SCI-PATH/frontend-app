"use client";

import { StudentLearningPath } from "@/components/features/learning-path-engine";

/**
 * Student learning viewport — thin route per DEVELOPER_README.
 * Domain UI lives in features/learning-path-engine.
 */
export default function LearningPathPage() {
  return <StudentLearningPath />;
}
