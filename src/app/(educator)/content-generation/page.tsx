"use client";

import { TeacherContentGeneration } from "@/components/features/learning-path-engine";

/**
 * Educator content-generation screens — thin route per DEVELOPER_README.
 * Domain UI lives in features/learning-path-engine.
 */
export default function ContentGenerationPage() {
  return <TeacherContentGeneration />;
}
