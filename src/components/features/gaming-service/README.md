# Gaming Service (Farm / DDA)

Phaser 3 + React Vite app for SCI-PATH. Lives under the gaming-service feature folder.

## Run

```bash
cd src/components/features/gaming-service
npm install
npm run dev
```

Pushed to branch `dev` of SCI-PATH/frontend-app only.
