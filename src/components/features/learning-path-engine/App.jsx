"use client";

/**
 * @deprecated Import StudentLearningPath from the feature index instead.
 * Kept so old relative imports of ./App still resolve to the student UI.
 */
export { default } from "./StudentLearningPath";
